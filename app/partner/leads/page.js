import Leads from "../../_components/leads";

export const metadata = {
    title: "Leads - Se alle tilgængelige leads her - Tagberegneren.dk",
    description:
        "Se alle tilgængelige leads her. Du kan se alle leads, der er tilgængelige for dig. ",
};

export default function LeadsPage() {
    return <Leads />;
}
